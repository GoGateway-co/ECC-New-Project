---
description: Create session handoff files and update roadmap for project continuity across sessions.
---

# /handoff

Create a session handoff. Preserves context, decisions, and next steps for continuity between Claude Code sessions.

Follow the instructions in `skills/handoff/SKILL.md` to execute the handoff workflow.

Pass through any flags from the user's prompt: `--init`, `--task <id>`, `--quick`, `--no-commit`, `--dry-run`, `--review`.
